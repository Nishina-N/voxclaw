{
  "name": "gtask_delete_task",
  "description": "Googleタスクを削除します。task_idは必須。削除前にユーザーに確認を取ってください。",
  "parameters": {
    "type": "OBJECT",
    "properties": {
      "task_id": {
        "type": "STRING",
        "description": "削除するタスクのID（get_tasksで取得可能）"
      },
      "tasklist_id": {
        "type": "STRING",
        "description": "タスクリストのID（省略時はデフォルトリスト）"
      }
    },
    "required": ["task_id"]
  }
}
