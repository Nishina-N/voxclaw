{
  "name": "gsheet_create_spreadsheet",
  "description": "Google スプレッドシートを新規作成します。タイトルは必須。シート名のリストを渡すと複数シートを初期作成できます。作成後に返されるspreadsheetIdを使って読み書きができます。",
  "parameters": {
    "type": "OBJECT",
    "properties": {
      "title": {
        "type": "STRING",
        "description": "スプレッドシートのタイトル"
      },
      "sheets": {
        "type": "ARRAY",
        "description": "初期シート名のリスト（省略時はデフォルトの「シート1」が作られる）。例: [\"1月\", \"2月\", \"3月\"]",
        "items": { "type": "STRING" }
      }
    },
    "required": ["title"]
  }
}
