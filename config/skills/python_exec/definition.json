{
  "name": "python_exec",
  "description": "Execute a Python code string and print the result.",
  "parameters": {
    "type": "OBJECT",
    "properties": {
      "code": {
        "type": "STRING",
        "description": "The Python code to execute."
      }
    },
    "required": ["code"]
  }
}
